{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": ".",
    "functions": {
        "api/**/*.js": {
            "runtime": "@vercel/node@3"
        }
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        }
    ]
}